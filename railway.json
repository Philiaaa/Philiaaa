{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export DATABASE_URL=${DATABASE_URL:-file:./prisma/dev.db} && prisma migrate deploy && next start",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
